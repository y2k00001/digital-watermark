server:
  port: 8055 # 服务端口

spring:
  profiles:
    active: dev # 环境设置
  datasource: # mysql数据库连接
    type: com.zaxxer.hikari.HikariDataSource
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/localdata?serverTimezone=GMT%2B8&characterEncoding=utf-8
    username: root
    password: root
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher   #swagger2，springboot2.6版本以上要加
  mail:
    host: smtp.qq.com
    username: 28*****99@qq.com
    password: ki*********ja

mybatis-plus: #mybatis
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  mapper-locations: classpath:/mapper/**/*.xml
acta  
# 可以设置为create（纯创建）、add（添加新的不删除内容）、update（更新内容，推荐用）、none（啥都不干）
  actable.table.auto = update
  # 用于创建数据表的待扫描目录，该目录下的类如果有@Table注解，就会进行扫描
  #你的实体类放的包
  actable.model.pack = com.forum.entities
  # 创建类型，目前仅支持mysql
  actable.database.type = mysql
  # mybatis-plus自有的配置信息，key也可能是：mybatis.mapperLocations
  #看你用的是mybatis-plus还是mybatis，为这里用的是mybatis-plus
  mybatis-plus.mapper-locations = classpath*:com/gitee/sunchenbin/mybatis/actable/mapping/*/*.xml


logging:
  level:
    - com.trace.core: debug


aliyun:
  oss:
    endpoint: oss-cn-guangzhou.aliyuncs.com
    keyId: LT*************zj
    keySecret: iym*************kE8
    bucketName: t*********t
